#ifndef sum_h
#define sum_h

/**
 * File: 4-sum.h
 * Desc: a function-like macro SUM(x, y)
 * that computes the sum of the numbers x and y.
 * @x: SUM(x)
 * @y: SUM(y)
 */

#define SUM(x, y) ((x) + (y))

#endif
